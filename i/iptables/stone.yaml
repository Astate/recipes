# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : iptables
version     : "1.8.13"
release     : 9
homepage    : https://www.netfilter.org/projects/iptables/
upstreams   :
    - https://netfilter.org/pub/iptables/iptables-1.8.13.tar.xz : 1afcd33da9e8f913ace6a2126788162e207e26f5d5e29c6573c0e581ffc58b99
summary     : Linux kernel packet control tool (using nft interface)
description : |
    Linux kernel packet control tool (using nft interface)
license     :
    - GPL-2.0-only
builddeps   :
    - pkgconfig(libpcap)
    - pkgconfig(libmnl)
    - pkgconfig(libnftnl)
    - linux-headers
setup       : |
    %configure \
        --sysconfdir=%(vendordir)%(sysconfdir) \
        --disable-static \
        --enable-bpf-compiler \
        --enable-ipv6 \
        --enable-libipq \
        --enable-shared
build       : |
    %make
install     : |
    %make_install

    # Use NFT kernel interface
    for i in iptables iptables-restore iptables-save ip6tables ip6tables-restore ip6tables-save; do
        rm -v %(installroot)/%(sbindir)/$i
        ln -s xtables-nft-multi %(installroot)/%(sbindir)/$i
    done
tune        :
    - optimize: size
