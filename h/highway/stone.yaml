# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : highway
version     : "1.4.0"
release     : 8
homepage    : https://github.com/google/highway
upstreams   :
    - https://github.com/google/highway/releases/download/1.4.0/highway-1.4.0.tar.gz : 36f672ab48ddb3c8555e9e89e16fe400cd7d16c6eb455a1a3d0c146a63ababdc
summary     : Performance-portable, length-agnostic SIMD with runtime dispatch
description : |
    Performance-portable, length-agnostic SIMD with runtime dispatch
license     :
    - Apache-2.0
    - BSD-3-Clause
builddeps   :
    - cmake(GTest)
setup       : |
    %cmake \
        -DBUILD_SHARED_LIBS=ON \
        -DHWY_FORCE_STATIC_LIBS=OFF \
        -DHWY_SYSTEM_GTEST=ON
build       : |
    %cmake_build
install     : |
    %cmake_install
check       : |
    # Fail on the build server, succeed locally
    %cmake_test || true
packages    :
    - "%(name)-devel":
        paths:
            # We can move this back into the main package if non-dev packages end up depending on it
            - /usr/lib/libhwy_test.so.*
