# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : pciutils
version     : "3.15.0"
release     : 8
homepage    : https://mirrors.edge.kernel.org/pub/software/utils/pciutils
upstreams   :
    - https://mirrors.edge.kernel.org/pub/software/utils/pciutils/pciutils-3.15.0.tar.xz : c02940f430841ecf158d5d9a50007afc4d5353c8678a2455003ca0b2c4e9f5ff
summary     : Utilities to access the PCI bus configuration registers
description : |
    The PCI Utilities package contains a library for portable access to PCI bus
    configuration registers and several utilities based on this library.
license     :
    - GPL-2.0-or-later
builddeps   :
    - binary(strip)
    - pkgconfig(libudev)
rundeps   :
    - hwdata
build       : |
    %make \
        OPT="$CFLAGS" \
        PREFIX=/usr \
        SHARED=yes \
        CC="$CC" \
        IDSDIR=/usr/share/hwdata \
        all
install     : |
    %make_install OPT="$CFLAGS" \
                  PREFIX=/usr \
                  SHARED=yes \
                  CC="$CC" \
                  IDSDIR=/usr/share/hwdata \
                  install-lib

    # pci.ids are handled by hwdata
    rm -rf %(installroot)/usr/share/hwdata
tuning      :
    # ld: error: undefined symbol: pci_alloc ... and so on
    - lto: false
packages    :
    - "%(name)-libs":
        paths:
            - /usr/lib/libpci.so.*
