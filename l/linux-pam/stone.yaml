# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : linux-pam
version     : "1.7.3"
release     : 21
summary     : Pluggable Authentication Modules
license     :
    - BSD-3-Clause
    - GPL-3.0-or-later
homepage    : http://www.linux-pam.org/
description : |
    Pluggable Authentication Modules
upstreams   :
    - https://github.com/linux-pam/linux-pam/releases/download/v1.7.3/Linux-PAM-1.7.3.tar.xz: 2ce4765fd49df6693771ef2941f81e33d8ee14b94a81a5c7b369aa3b137b85a5
builddeps   :
    - binary(bison)
    - binary(flex)
    - binary(gettext)
    - binary(xsltproc)
    - pkgconfig32(libeconf)
    - pkgconfig32(libsystemd)
    - pkgconfig32(libxcrypt)
    - docbook-xsl
    - docbook5-xml
emul32      : true
profiles    :
    - emul32:
        install: |
            %meson_install

            # _PROBABLY_ not needed, re-add if that assumption is false
            rm -rfv %(installroot)%(libdir)/security
environment : |
    %export_docbook_catalogs
setup       : |
    %meson \
        -Dvendordir=%(vendordir)
build       : |
    %meson_build
install     : |
    %meson_install

    %tmpfiles "# Create run/faillock directory"
    %tmpfiles "d /run/faillock 0755 root root -"

    # add ability to check pwd for any user instead only the current (required by cosmic-greeter)
    chmod u+s %(installroot)/usr/sbin/unix_chkpwd
packages    :
    - "%(name)-docs":
        paths:
            - /usr/share/doc

    - "%(name)-libs":
        summary: "Library files for %(name)"
        description: |
            Library files for %(name), typically pulled in as a dependency of another package.
        paths:
            - /usr/lib/lib*.so.*

    - "%(name)":
        rundeps:
            - linux-pam-base
