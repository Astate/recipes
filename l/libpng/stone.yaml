# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : libpng
version     : "1.6.58"
release     : 16
homepage    : http://www.libpng.org/pub/png/libpng.html
upstreams   :
    - https://downloads.sourceforge.net/project/libpng/libpng16/1.6.58/libpng-1.6.58.tar.xz : 28eb403f51f0f7405249132cecfe82ea5c0ef97f1b32c5a65828814ae0d34775
summary     : Official reference implementation of PNG
description : |
    libpng is the official PNG reference library. It supports almost all PNG features, is extensible,
    and has been extensively tested for over 23 years.
license     :
    - GPL-2.0-or-later
    - JSON
    - libpng-2.0
emul32      : true
packages    :
    - "%(name)-devel":
        paths:
            # libpng-config, etc.
            - /usr/bin
builddeps   :
    - pkgconfig(zlib)
    - pkgconfig32(zlib)
setup       : |
    # breaks libpng
    unset CPP
    %configure \
        --disable-static \
        --enable-intel-sse
build       : |
    %make
install     : |
    # sometimes install silently fails o_O
    %make_install -j1
check       : |
    # emul32 pngtest-all fails
    %make check || :
tuning      :
    - lto : full
