# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : libphonenumber
version     : "9.0.37"
release     : 15
upstreams   :
    - https://github.com/google/libphonenumber/archive/refs/tags/v9.0.37.tar.gz : 4bd34bc3aec5a89dc9035d15aaf2256c5e8f7e0524e6eb3e7bac1e811987c230
homepage    : https://github.com/googlei18n/libphonenumber
license     :
    - Apache-2.0
    - BSD-3-Clause
summary     : library for working with international phone numbers
description : |
    Google's common library for parsing, formatting, and validating international phone numbers
builddeps   :
    - binary(java-25)
    - cmake(Boost)
    - cmake(absl)
    - cmake(protobuf)
    - pkgconfig(icu-i18n)
    - pkgconfig(icu-uc)
environment : |
    export JAVA_HOME=%(libdir)/openjdk-25
    export PATH=$JAVA_HOME/bin:$PATH
setup       : |
    %patch %(pkgdir)/remove-werror.patch
    %patch %(pkgdir)/0003-Fix-BUILD_STATIC_LIB-OFF.patch
    %patch %(pkgdir)/ftbfs-boost-1.89.patch

    %cmake -S cpp \
        -DBUILD_STATIC_LIB=OFF
build       : |
    %cmake_build
install     : |
    %cmake_install
check       : |
    cmake --build %(builddir) --target tests
packages    :
    - "%(name)-devel":
        rundeps:
            - cmake(Boost)
            - cmake(absl)
            - cmake(protobuf)
