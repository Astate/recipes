# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : wireplumber
version     : "0.5.16"
release     : 16
homepage    : https://gitlab.freedesktop.org/pipewire/wireplumber/
upstreams   :
    - https://gitlab.freedesktop.org/pipewire/wireplumber/-/archive/0.5.16/wireplumber-0.5.16.tar.gz : 59f2f72129dc385e13aaac3820122c6927d783c9699811b56f13a74296993d3a
summary     : Session / policy manager implementation for PipeWire
description : |
    Session / policy manager implementation for PipeWire
license     :
    - MIT
builddeps   :
    - binary(doxygen)
    - binary(g-ir-scanner)
    - binary(msgfmt)
    - binary(sphinx-build)
    - pkgconfig(gio-2.0)
    - pkgconfig(glib-2.0)
    - pkgconfig(gmodule-2.0)
    - pkgconfig(gobject-2.0)
    - pkgconfig(libpipewire-0.3)
    - pkgconfig(libspa-0.2)
    - pkgconfig(libsystemd)
    - pkgconfig(lua)
    - pkgconfig(systemd)
    - python(lxml)
    - python(sphinx)
    - python(sphinx-rtd-theme)
rundeps     :
    - binary(pipewire)
setup       : |
    #https://github.com/ValveSoftware/steam-for-linux/issues/11901
    %patch %(pkgdir)/block_steam_clear_default.patch

    %meson \
        -Dsystem-lua=true
build       : |
    %meson_build
install     : |
    %meson_install
packages    :
    - "%(name)-libs":
        summary: "Library files for %(name)"
        description: |
            Library files for %(name), typically pulled in as a dependency of another package.
        paths:
            - /usr/lib/wireplumber-0.5/libwireplumber-*.so
            - /usr/lib/libwireplumber-0.5.so.*
            - /usr/lib/girepository-1.0/Wp-0.5.typelib

    # Moss upgrade issue
    - "%(name)":
        rundeps:
            - "%(name)-libs"

    - "%(name)-devel":
        paths:
            # Remove once https://github.com/AerynOS/os-tools/pull/491 is merged
            - /usr/share/gir-1.0/*.gir
