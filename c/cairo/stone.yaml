# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : cairo
version     : "1.18.6"
release     : 10
homepage    : https://cairographics.org
upstreams   :
    - https://cairographics.org/releases/cairo-1.18.6.tar.xz : 1c767308174337a74694da0f3ec069c271452163a1ef4540964c50c301f157d4
summary     : Cairo is a 2D graphics library with support for multiple output devices.
description : |
    Cairo is a 2D graphics library with support for multiple output devices. Currently supported output targets include the X Window System (via both Xlib and XCB), Quartz, Win32, image buffers, PostScript, PDF, and SVG file output. Cairo is designed to produce consistent output on all output media while taking advantage of display hardware acceleration when available (eg. through the X Render Extension).
license     :
    - GPL-3.0-or-later
    - LGPL-2.1-or-later
    - MPL-1.1
builddeps   :
    - binary(gs)
    - pkgconfig(fontconfig)
    - pkgconfig(freetype2)
    - pkgconfig(glib-2.0)
    - pkgconfig(gobject-2.0)
    - pkgconfig(libpng)
    - pkgconfig(librsvg-2.0)
    - pkgconfig(libspectre)
    - pkgconfig(lzo2)
    - pkgconfig(pixman-1)
    - pkgconfig(poppler-glib)
    - pkgconfig(x11)
    - pkgconfig(xcb-render)
    - pkgconfig(xcb-shm)
    - pkgconfig(xext)
    - pkgconfig(xrender)
    - pkgconfig(zlib)
setup       : |
    %meson
build       : |
    %meson_build
install     : |
    %meson_install
packages    :
    - "%(name)-gobject":
        paths:
            - /usr/lib/libcairo-gobject.so.*

    - "%(name)-gobject-devel":
        rundeps:
            - "%(name)-gobject"
        paths:
            - /usr/include/cairo/cairo-gobject.h
            - /usr/lib/pkgconfig/cairo-gobject.pc
            - /usr/lib/libcairo-gobject.so

    - "%(name)-devel":
        paths:
            - /usr/bin/
            - /usr/lib/cairo/
