# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : mangohud
version     : "0.8.4"
release     : 15
homepage    : https://github.com/flightlessmango/MangoHud
upstreams   :
    - https://github.com/flightlessmango/MangoHud/releases/download/v0.8.4/MangoHud-v0.8.4-Source.tar.xz : bbe5a2b976313c53f21dc24b98bbed03226589460a0cce20d923cb2c36c9d8b0
summary     : A Vulkan and OpenGL overlay for monitoring FPS, temperatures, CPU/GPU load and more
description : |
    A Vulkan and OpenGL overlay for monitoring FPS, temperatures, CPU/GPU load and more.
license     : MIT
builddeps   :
    - binary(glslang)
    - pkgconfig(cmocka)
    - pkgconfig(dbus-1)
    - pkgconfig(gl)
    - pkgconfig(glfw3)
    - pkgconfig(spdlog)
    - pkgconfig(wayland-client)
    - pkgconfig(x11)
    - pkgconfig(xkbcommon)
    - pkgconfig(xrandr)
    - python(mako)
    - libstdc++-static
setup       : |
    %meson \
        -Dmangoapp=true \
        -Dmangohudctl=true \
        -Duse_system_spdlog=enabled \
        -Dwith_xnvctrl=disabled # for now. needs nvidia-settings
build       : |
    %meson_build
install     : |
    %meson_install --skip-subprojects
packages    :
    - mangoplot:
        # This needs matplotlib and numpy
        paths:
            - /usr/bin/mangoplot
