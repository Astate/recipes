# SPDX-FileCopyrightText: 2025 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : flatpak-builder
version     : "1.4.11"
release     : 4
upstreams   :
    - https://github.com/flatpak/flatpak-builder/releases/download/1.4.11/flatpak-builder-1.4.11.tar.xz : 2a5ef4f3eaea86f808aa477e57e65b6f68978eb1c034fafe05bffdebbf1dcd03
homepage    : https://flatpak.org
license     : LGPL-2.1-or-later
summary     : Tool to build flatpaks from source
description : |
    Flatpak-builder is a tool for building flatpaks from sources
builddeps   :
    - binary(debugedit)
    - binary(xmlto)
    - binary(xsltproc)
    - pkgconfig(glib-2.0)
    - pkgconfig(gio-2.0)
    - pkgconfig(gio-unix-2.0)
    - pkgconfig(json-glib-1.0)
    - pkgconfig(libcurl)
    - pkgconfig(libelf)
    - pkgconfig(libxml-2.0)
    - pkgconfig(ostree-1)
    - pkgconfig(yaml-0.1)
    - appstream-compose
    - docbook
    - docbook-xsl
rundeps     :
    - binary(debugedit)
    - binary(rofiles-fuse)
    - appstream-compose
environment : |
    %export_docbook_catalogs
setup       : |
    # glibc 2.43 build failure, remove in future version
    # export CFLAGS+=" -Wno-error=incompatible-pointer-types"

    %meson
build       : |
    %meson_build
install     : |
    %meson_install
