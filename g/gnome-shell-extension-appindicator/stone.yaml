# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : gnome-shell-extension-appindicator
version     : "66"
release     : 10
homepage    : https://github.com/ubuntu/gnome-shell-extension-appindicator
upstreams   :
    - https://github.com/ubuntu/gnome-shell-extension-appindicator/archive/refs/tags/v66.tar.gz : 3f692824e54c2192d984a24e1685722d1c88787e24dba662aecabe0b7f9e2bbc
summary     : AppIndicator / SNI support in GNOME Shell
description : |
    AppIndicator / SNI support in GNOME Shell
license     :
    - GPL-2.0
builddeps   :
    - binary(glib-compile-schemas)
    - binary(jq)
    - binary(msgfmt)
setup       : |
    %meson \
        -Dlocal_install=disabled
build       : |
    %meson_build
install     : |
    %meson_install
    rm %(installroot)%(datadir)/glib-2.0/schemas/*.compiled -v
