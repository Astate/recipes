# SPDX-FileCopyrightText: 2024 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

name        : gnome-session
version     : "51.0"
release     : 32
homepage    : https://gitlab.gnome.org/GNOME/gnome-session
upstreams   :
    - https://download.gnome.org/sources/gnome-session/51/gnome-session-51.0.tar.xz : 5195c4f60542a953ab6ebd288f3785b8e7746292d8c167e0c99d353438a184d1
summary     : GNOME session manager
description : |
    gnome-session contains the GNOME session manager, as well as a configuration program to choose applications starting on login.
license     :
    - GPL-2.0-or-later
builddeps   :
    - binary(msgfmt)
    - binary(xmlto)
    - binary(xsltproc)
    - pkgconfig(gnome-desktop-4)
    - pkgconfig(gtk4)
    - pkgconfig(json-glib-1.0)
    - pkgconfig(upower-glib)
    - docbook
    - docbook-xsl
rundeps    :
    - binary(boltctl)
    - binary(gnome-keyring)
    - binary(switcherooctl)
    - binary(update-desktop-database)
    - binary(update-mime-database)
    - binary(xdg-open)
    - binary(xdg-user-dirs-gtk-update)
    - pciutils
    - power-profiles-daemon
    - sound-theme-freedesktop
    - wireplumber
environment : |
    %export_docbook_catalogs
setup      : |
    %meson
build      : |
    %meson_build
install    : |
    %meson_install

    # Default mimeapps for GNOME
    %install_file %(pkgdir)/gnome-mimeapps.list -t %(installroot)/usr/share/applications
